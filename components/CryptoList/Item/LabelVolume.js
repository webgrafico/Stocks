import styled from 'styled-components';
import { FONT_SIZE } from '../../../constants';

const LabelVolume = styled.div`
  font-size: ${FONT_SIZE.XXXS};
  margin-bottom: 4px;
  color: rgb(111, 112, 117);
`;

export default LabelVolume;
